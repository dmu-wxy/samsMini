<!--pages/map/map.wxml-->
<map
      id="myMap"
      style="width:{{ScreenTotalW}}rpx;height:{{ScreenTotalH}}rpx"
      latitude="{{latitude}}"
      longitude="{{longitude}}"
      markers="{{markers}}"
      covers="{{covers}}"
      show-location
    ></map>